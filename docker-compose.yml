# docker-compose.yml
version: '3'
services:
  app:
    build: .
    working_dir: /mnt
    volumes:
      - .:/mnt
    environment:
      PYTHONPATH: .
      PYTHONUSERBASE: /mnt/vendor
      TELEGRAM_TOKEN: 
    command: python main.py